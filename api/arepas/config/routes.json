{
  "routes": [
    {
      "method": "GET",
      "path": "/arepas",
      "handler": "arepas.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/arepas/count",
      "handler": "arepas.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/arepas/:id",
      "handler": "arepas.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/arepas",
      "handler": "arepas.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/arepas/:id",
      "handler": "arepas.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/arepas/:id",
      "handler": "arepas.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
